// This file holds shared configuration values used throughout the app.
// Keeping the API URL here means you only change it in one place when you deploy.
// Currently points to your local machine's IP on port 3000 (the Express backend).
export const API_URL = 'http://192.168.2.140:3000';
